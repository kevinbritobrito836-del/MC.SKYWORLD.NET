#✦------------------------------------------------------✦
#|                   Scoreboard Config                   |
#✦------------------------------------------------------✦
# RealScoreboard created by joserodpt and maintained by Neziw
# Parameter explanation:
# - Default: if the scoreboard is the default one, doesn't require a permission
# - Default-World: the default world where the scoreboard appears
# - Display-Name: used for GUI and other purposes.
# - Refresh-Scoreboard: the main refresh delay for the whole scoreboard
# - Refresh-Title: refresh delay only for the scoreboard title
# - Title-Loop-Delay: loop delay for the title animation task
# - Board-Loop-Delay: loop delay for the bards switching task (if the scoreboard has multiple boards)
Version: 3
Scoreboards:
    Lobby:
    Default: true
    Default-World: world
    Other-Worlds:
    - '*'
    Display-Name: '&4Default'
    Permission: none
    Refresh:
      Scoreboard: 2
      Title: 10
      Title-Loop-Delay: 7
      Board-Loop-Delay: 30
    Boards:
      Board1:
        Title:
        - '&b&lSKYWORLD NETWORK '
        - '&6> &4&lSKYWORLD NETWORK &6<'
        - '&a>&6> &5&lSKYWORLD NETWORK &6<&a<'
        - '&6> &a&lSKYWORLD NETWORK &6<'
        Lines:
        - '&a&l>> Jugador << '
        - ' &fNombre: &f%playername% '
        - ' &fRango: &f%group%'
        - ' &fDinero: &f%money% '
        - ' '
        - '  &B&L>> ESTADISTICAS <<'
        - '&f&lPing: %ping%'
        - ' &f&lHoras: &f%playtime%'
        - ' &f&lKills: %Kills%'
        - ' &f&lMuertes: %Deaths%'
        - ' ' 
        - ' &4&l>> ServerInfo <<'
        - ' &f&lConectados: %online%/%maxplayers% '
        - ' &f&lPuerto: 25565 ' 
        - ' &f&lIP: jun-ol.gl.joinmc.link ' 
         